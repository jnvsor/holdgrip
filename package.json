{
    "name": "holdgrip",
    "devDependencies": {
        "sass": "^1.77.6",
        "normalize.css": "^8"
    },
    "scripts": {
        "build": "sass -s compressed --no-source-map sass/:html/"
    }
}
